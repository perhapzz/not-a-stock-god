version: "3.8"

services:
  backend:
    build: ./backend
    container_name: not-a-stock-god-api
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/game.db
    restart: unless-stopped
